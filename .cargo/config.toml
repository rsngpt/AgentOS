# Cross-compile the guest agent to a fully static aarch64 musl binary using
# rust-lld, so no external cross toolchain is needed on macOS hosts.
[target.aarch64-unknown-linux-musl]
linker = "rust-lld"
rustflags = ["-C", "target-feature=+crt-static", "-C", "link-self-contained=yes"]
